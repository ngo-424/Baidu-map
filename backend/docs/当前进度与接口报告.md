# 当前进度与接口报告

> 2026-09-13 18:24 恢复完成：补测 15 点，固定边界参考 93/96 有效（55 可达、38 不可达）；自适应／均匀／扇形确认请求 400／400／247 次，耗时 192.125／234.891／163.891 秒，均为 partial。边界漏纳 14/55、12/55、18/55；误纳 4/45、5/48、1/38；unknown 11/93、8/93、20/93。本次新增 1062 次，累计预留 1144、确认发送 1143，零重试、无超频或记录故障。259 项回归通过，算法与 API 未修改。详见 [边界比较恢复报告](国定一社区_边界比较恢复报告.md)；下方中断记录保留作历史证据。

> 最新：comparison-v1 已冻结为 f2a3a76 并发起 PR #15。边界 96 点已固定，工具提交 952532b；真实参考阶段因本地账本写入失败停止，82 次预算预留、81 次确认发送、80 个有效参考（45 可达／35 不可达），三算法均未执行。Windows 账本读句柄干扰已离线复现，改用进程输出监控，补齐错误码与阶段状态修复；最终 Python 251 项通过。未追加真实请求，算法与正式 API 保持不变。详见 [边界比较报告](国定一社区_边界独立参考集比较报告.md)。

## 2026-09-13 代码核验与远程同步

本节为当前实现说明，后文原接入记录保留为历史证据；其中“设施尚未接入”的固定描述不再适用于真实业务分支。任务 A 的稳定性与三算法比较成果已随团队主分支同步，不再将其列为尚未开展。

- 修复分支：`review/20260913-code-quality`，同步团队 `d2a8e10`。保留原 PR #13 的未合并实现并重新提交团队评审。
- 设施请求统一持有响应周期名额，发送前再次检查取消与截止；权限、配额和限流错误停止后续设施发送。单页解析、分页决策、分类与实体合并已分离。
- 查询成功不代表现实目录完整。重复页、变化总数、提前空页和召回保护上限保留受限原因；冲突 UID 不计入设施。
- 有独立有效步行证据的采样点可记录 covered；其余为 unknown，不再用有限关键词检索的空结果断言 blind。`serviceBlindRegions` 保留字段且返回空几何，不再从采样点外推 180 米盲区面积。
- 保留现有五类兼容接口、三大类展示、5/10/15 分钟图层与路线入口。后续 B1 的三类标准数据层将独立实现，不据 B1 离线验收修改当前业务接入状态。
- 合并处理保留团队新增报告、时间戳/中心校验、地图初始化修复及全部任务 A 审计文件；结果适配器保留设施与时间图层字段。中心改变时地图与设施一起失效，历史报告单独保留并提示原条件。
- 测试：后端 195、算法 96、前端单元 135 项通过；生产构建通过。浏览器集成 19 项通过（18 项首轮通过，1 项修正重复文本定位后复测通过），报告浏览器 4 项通过。所有调用均使用合成 Provider 或模拟传输，真实百度请求 0。
- 限制：当前设施业务仍是有限范围检索与采样点核验，未建立全量目录或面积盲区证据；跨机器共享账号限流仍需错峰协调。

---

> 2026-09-13 后续进度：已从固定标签完成 256 次短时稳定性验证和三算法各 400 次预算上限的真实比较。稳定性全部通过；243 个共同有效参考点上，自适应/均匀/扇形分别漏纳 2/28、3/28、9/28 个可达点，三者均为 partial。保守预算 1559/2000 次，确认发送 1558 次。发现并离线修复实验工具记录失败未明确停止、预算预留误标为发送的问题，最终 Python 230 项通过，未追加真实请求。详见 [稳定性与三算法比较报告](国定一社区_稳定性与三算法比较报告.md)。下文为固定基线交付时的进度与接口，正式 API 保持不变。

更新：2026-09-13。远端基线 `43da3af2baa24d87fc478d088394b700e708fb5f`；交付分支 `codex/guodingyi-live-smoke`，目标 `main`。最新拉取检查确认基线未变化。本报告随 PR 交付，具体合入版本以 PR 提交记录为准。

## 1. 当前进度

已打通“百度底图选点／坐标输入 → FastAPI 任务 → 自适应算法 → 结果轮询 → 几何展示 → 取消及错误恢复”。真实社区已完成首轮链路测试，尚未建立独立参考集，不能报告真实社区 IoU、边界 P95 或步行准确率。

| 模块 | 已实现与已验证 | 尚未完成 |
| --- | --- | --- |
| 算法 | 145 个初始化位置含原点锚点；400→50 米四叉树、共享样本、边界主动补测、探索、范围扩展；三角插值及 ContourPy 区域提取，保留未知缺口、孔洞和多分量 | 真实社区独立精度对照、现场核验 |
| 调度 | 缓存与去重、预算、最多两次尝试、超时、截止、取消及连续故障停止；确定性批次更新 | 跨进程、跨机器统一限流 |
| 百度适配器 | BD09LL、900 秒业务阈值、最短有效路线、分段端点核验；兼容数值字符串坐标、50 米偏移检查 | 长期及不同网络出口的稳定性验证 |
| 后端任务 | 单进程一个运行任务、幂等创建、查询、结果、取消和请求标识恢复；几何重建移出事件循环 | 数据库持久化、任务队列、鉴权及公网部署 |
| 前端 | API 默认模式、显式演示模式、每秒轮询、取消旧任务、防止旧结果覆盖；可达/未知/不确定/范围图层，空几何与 null 区分 | 真实设施统计、盲区评价及设施报告 |
| 实验 | 合成场景、三算法、200/400/800 预算及消融入口；首轮真实地图与任务链路 | 正式真实预算比较及消融 |

真实模式的 `facilitiesStatus` 为 `not_integrated`，界面显示“尚未接入”；演示和 Mock 数据不能当作社区设施结论。

## 2. 最新真实证据与修复

中心：上海市杨浦区五角场街道国定一社区，政立路 570 号附近；用户提供 BD09LL 经度 `121.513926`、纬度 `31.313077`。首次地图显示已核对，无擅自修改坐标。

| 阶段 | 调用 | 实际结论 |
| --- | ---: | --- |
| 首轮冒烟及端点诊断 | 9 | 发现实际端点为数值字符串，适配器修复后核验通过 |
| 首轮取消 | 1 | cancelled，取消后没有新发送，结果读取 409 |
| 首轮完整分析 | 200 | completed，算法 partial/budget，69 秒、2 个分量、0 孔洞；未知面积约 1.765 平方公里，占范围约 17.24% |
| 提前唤醒修复后复核 | 18/20 | 本地时序通过，但有 2 次业务 401，重试成功 |
| 响应后节流修复后复核 | 16/20 | 全部有效且端点核验通过，0 限流、0 重试；最大在途 1、滚动一秒最多 3 |

首轮前三项合计 **210/220**。三轮合计 **244 次**，各轮预算独立，旧账本保持不变，没有追加第二个 200 次任务。完整分析结果来自旧限流版本，不能把它的耗时当作新策略性能；新版本此次只复核小样本。

两个 QPS 缺口已修复：提前唤醒后未复查，以及放行间隔未覆盖连接耗时。真实 Provider 现在串行持有名额直到响应结束，再至少等待 `1/QPS`；限流、超时及中断后至少冷却 1 秒。QPS=3 表示上限，实际吞吐量低于 3。具体复现、旧失败事实及最新线上指标见 [QPS 报告](QPS限流问题报告.md)。

## 3. 正式前端使用的任务接口

本地基地址默认 `http://127.0.0.1:8000`。业务坐标为 `[经度, 纬度]`，必须显式 `bd09ll`；不要传 WGS84/GCJ02 或演示画布坐标。

| 方法与路径 | 请求/响应 | HTTP 状态 |
| --- | --- | --- |
| `POST /api/analyses` | 提交中心、坐标系、预算、客户端请求标识，返回任务视图 | 202；输入错 422；忙或标识冲突 409；真实配置不足 503 |
| `GET /api/analyses/{taskId}` | 查询任务视图 | 200；不存在/过期 404 |
| `GET /api/analyses/{taskId}/result` | 读取完成结果 | 200；未完成/失败/取消 409；不存在 404 |
| `POST /api/analyses/{taskId}/cancel` | 无请求体，幂等取消，返回任务视图 | 202；不存在 404 |
| `POST /api/analyses/by-request/{clientRequestId}/cancel` | 丢失创建响应时按原标识取消，不创建新任务 | 202；不存在 404 |
| `GET /health` | `status`、`baidu_ak_configured` 布尔值 | 200；不证明步行权限和白名单通过 |

创建请求示例：

```json
{
  "center": {"lng": 121.513926, "lat": 31.313077},
  "coordinateSystem": "bd09ll",
  "budget": 200,
  "clientRequestId": "guodingyi-example-001"
}
```

预算只支持 **200/400/800**，默认 400。未知字段、字符串坐标、布尔预算不接受。中心归一化为六位小数。同一标识和相同归一化中心、预算复用任务；同标识用于不同参数返回 409。幂等性仅在任务仍保留时有效，30 分钟过期或服务重启后不再保证复用。

任务视图字段：`taskId`、`status`、`stage`、`requests`、`networkRequests`、`budget`、`elapsedSeconds`、`dataSource`、`error`。状态为 `running / cancelling / completed / cancelled / failed`；运行阶段包括 `initializing / expanding / exploring / refining / reconstructing`。进度表示真实阶段和计数，不是估算百分比。

| 完成结果字段 | 含义 |
| --- | --- |
| `taskId / dataSource / center / generatedAt` | 任务、`baidu_walking` 或 `synthetic` 来源、BD09LL 中心、Unix 秒时间戳 |
| `facilitiesStatus` | 当前固定 `not_integrated` |
| `isochrone.coordinateSystem` | 固定 `bd09ll` |
| `isochrone.geometry` | 可达 Polygon/MultiPolygon；有效空集合与 `null` 不同，null 表示证据不足 |
| `unknownRegion / uncertainRegion / computationExtent` | 位于 isochrone 内，分别为未知、不确定及计算范围几何 |
| `quality` | `usable / partial / insufficient`，也是 isochrone 字段 |
| `stopReason / warnings` | 真实停止原因与边界提示，例如 budget、deadline、range_truncated、unfinished_boundary，不应被前端改写 |
| `statistics / config` | 调用、重试、未知面积（平方米）、未完成边界等统计及实际算法配置 |

任务 `completed` 表示算法执行结束，不代表几何必然完整：partial 和 insufficient 仍可作为完成结果返回。`geometry_error` 或执行异常才单独失败。取消后的任务不提供完成结果，迟到响应不能覆盖终态。

`networkRequests` 是 Scheduler 的尝试预留计数；在 Provider 限流等待期间取消，可能已预留但尚未真正发送。真实费用审计以发送前持久化账本及交接记录为准，不能只凭前端计数认定百度计费。

当前每进程最多一个任务运行，忙时不排队。终态保留 30 分钟、最多 20 条，重启不恢复。CORS 默认允许 `http://127.0.0.1:5173` 与 `http://localhost:5173` 的 GET/POST，不提供身份认证。

## 4. 并存的 N05 契约接口

| 路径 | 用途及差异 |
| --- | --- |
| `GET /api/v1/analysis/mock/{scenario}` | scenario 为 complete/partial/failed/empty，固定 Mock，无真实步行 |
| `POST /api/v1/analysis/synthetic` | 同步返回合成计算结果；origin、coordinate_system、scenario、budget；场景 plane/local_failure/global_failure，预算整数 144～800 |

这套接口使用 `schema_version=1.0`、`analysis_id`、`coordinate_system` 和 `data`，来源为 mock/synthetic/system；状态是 complete/partial/failed/empty。它与任务接口的 camelCase 字段、completed 状态及 baidu_walking 来源不同，当前不提供真实异步任务生命周期。示例和完整模型见 [N05 契约](N05-接口契约.md)，在线 OpenAPI 位于 `/docs`。正式前端的等时圈流程使用第 3 节接口。

## 5. 配置与可复制命令

服务端配置位于 `backend/.env`：`ANALYSIS_PROVIDER=baidu`、`ANALYSIS_QPS=3`、`BAIDU_MAP_AK`。浏览器配置位于 `life-circle-demo/.env.local`：`VITE_ANALYSIS_MODE=api`、`VITE_API_BASE_URL=http://127.0.0.1:8000`、`VITE_BAIDU_MAP_AK`。两种 AK 分开配置，文件不进 Git。报告不包含真实值；配置变化需重启对应服务。

仓库根目录安装和离线启动（已有虚拟环境时跳过创建）：

```powershell
New-Item -ItemType Directory -Force D:/CodexCaches,D:/CodexTemp | Out-Null
$env:TEMP = 'D:/CodexTemp'
$env:TMP = 'D:/CodexTemp'
$env:PYTHONUTF8 = '1'
$env:PYTHONPYCACHEPREFIX = 'D:/CodexCaches/baidu-pycache'
$env:PIP_CACHE_DIR = 'D:/CodexCaches/pip'
if (-not (Test-Path D:/CodexCaches/baidu-map-algorithm-venv/Scripts/python.exe)) {
  py -3.11 -m venv D:/CodexCaches/baidu-map-algorithm-venv
}
$analysisPython = 'D:/CodexCaches/baidu-map-algorithm-venv/Scripts/python.exe'
& $analysisPython -m pip install -r backend/requirements.lock.txt -e ./life-circle-algorithm
$env:PYTHONPATH = (Resolve-Path backend).Path
$env:ANALYSIS_PROVIDER = 'synthetic'
& $analysisPython -m uvicorn app.main:app --host 127.0.0.1 --port 8000 --workers 1 --no-access-log
```

真实使用时确认 `.env` 配置后，移除当前进程的 `ANALYSIS_PROVIDER=synthetic` 覆盖再启动；环境变量优先于文件。不启用多 worker 或热重载运行实验。真实测试应使用持久化预算入口，普通任务接口本身不提供跨进程的多轮总账上限。

另开终端启动前端（本机依赖已经存在，本次没有重新安装）：

```powershell
Set-Location life-circle-demo
$env:npm_config_cache = 'D:/CodexCaches/npm'
npm run dev
```

回归命令，在根目录执行：

```powershell
$env:PYTHONPATH = (Resolve-Path backend).Path
$env:PYTHONPYCACHEPREFIX = 'D:/CodexCaches/baidu-pycache'
& D:/CodexCaches/baidu-map-algorithm-venv/Scripts/python.exe -m pytest backend/tests life-circle-algorithm/tests -q -o cache_dir=D:/CodexCaches/live-pytest --basetemp=D:/CodexCaches/qps-delivery-tests
Push-Location life-circle-demo
npm test
$env:VITE_BAIDU_MAP_AK = 'build-placeholder'
npm run build -- --outDir D:/CodexOutputs/guodingyi-qps-response-paced-review/frontend-dist
Remove-Item Env:VITE_BAIDU_MAP_AK
$env:INTEGRATION_OUTPUT_DIR = 'D:/CodexOutputs/guodingyi-qps-response-paced-review/integration'
npm run test:integration
Pop-Location
```

## 6. 本次验收与限制

| 验收 | 结果 |
| --- | --- |
| Python 全量 | 212 项通过，两项既有弃用提示 |
| 前端单元 | 106 项通过 |
| 浏览器集成 | 9 项通过；真实 FastAPI、合成 Provider、模拟地图 SDK，不访问百度 |
| 前端构建 | 通过，输出在 D 盘，使用占位浏览器 AK |
| 匀速平面 800 预算 | 5 种配置运行完成；自适应 IoU 0.99944057、边界 P95 0.65843 米，达到既定门槛 |
| 修复后真实 QPS | 16 次全部通过，无限流和重试 |

匀速实验仍如实标记 partial/budget 和 unfinished_boundary；精度门槛通过不覆盖质量标记。所有真实社区精度、现场步行核验、POI 完整性和设施统计仍未验收。

## 7. 下一步建议

以下为基线交付时的原计划；短时稳定性、固定独立点集和首轮 400 预算比较现已执行。下一步优先审查并锁定实验工具的记录保护修复，补充边界附近独立参考点，再安排跨时段重复或扩大预算，不能由本次一个点的差异认定自适应稳定优于均匀网格。

1. 合并 PR 后锁定实验提交与依赖版本；在同一服务端出口、单进程条件下先做一轮有明确预算的连续稳定性验证，对照百度控制台时间图，确认新限流策略的耗时和故障率。
2. 固定国定一社区的独立验证点集：道路可通行区、边界内外、围墙/入口、跨路通道与未知区域均覆盖；验证点不得参与算法采样，所有测时使用同一 Provider 和坐标口径，采集时间单独记录。真实调用预算包含验证点、失败和重试，先形成清单再运行。
3. 先比较自适应、均匀网格和扇形三算法各 400 次预算。仅训练/采样部分最多 1200 次尝试，独立参考集另计；不能把 1200 当作整个实验总预算。统一范围、阈值、参考集及时间窗口，报告误纳、漏纳、边界误差、拓扑、未知比例、成本与失败原因。
4. 数据质量及参考覆盖足够后，再扩展 200/400/800 和关闭主动补测、关闭探索的消融。点集只能支持点分类误差时，不冒称面积 IoU；正式社区 IoU/P95 需要足够独立的面与边界参考证据。
5. 算法稳定后再接真实设施检索、去重、圈内判定与设施统计，另立验收范围。N05 与任务 API 的统一作为后续兼容性工作，不在本次 PR 中改动前端契约。
