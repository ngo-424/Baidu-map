# N01：阶段 0 口径确认记录

确认日期：2026-09-13（Asia/Shanghai）
当前状态：**部分完成，待补充部署与其余业务口径**

## 已确认

| 项目 | 当前结论 | 影响 |
| --- | --- | --- |
| 15 分钟时间边界 | 使用 `≤900 秒`，恰好 900 秒视为可达 | 算法时间判断和边界样例可按此口径执行 |
| 1 公里距离类型 | 使用步行路线距离，不使用直线距离替代 | 设施覆盖/盲区判定必须等待真实步行路线结果 |
| 1 公里等号 | 采用 `≤1000 米`，恰好 1000 米算满足 | 距离规则按包含边界实现 |
| 距离容差 | 采用 100 米 | 落入容差带时标记为无法判断，不直接判为有设施或盲区 |
| 盲区评估范围 | 只判断 15 分钟可达区域内 | 可达区域之外不生成 1 公里盲区结论 |
| 固定样例点 | BD09LL，经度 `121.526090`、纬度 `31.259560` | 随机点仅作补充测试 |
| 设施分类 | 暂定采用一级大类、二级细分 | 购物（菜市场、超市）、医疗服务（药店、医院药房）、教育（小学）；幼儿园不归入小学 |
| 当前运行方式 | 现阶段仅在团队成员各自本地开发和测试 | 当前不能作为部署验收证据 |

## 尚待确认

| 项目 | 当前状态 | 需要补充 |
| --- | --- | --- |
| 真实调用限制 | 待确认 | 服务端 QPS 仍按百度控制台配额确定；本项目不以付费为前提，但总调用数、限流和重试仍需受控 |
| 部署平台 | 本阶段暂不验收 | 后续确定云平台或服务器方案 |
| 后端地址 | 本阶段暂不验收 | 部署后补充 HTTPS API 地址 |
| 前端域名 | 本阶段暂不验收 | 部署后补充 Web 访问域名 |
| HTTPS 方案 | 本阶段暂不验收 | 部署后补充证书、反向代理和域名配置方式 |

## 现阶段证据与限制

- 本地百度浏览器端 AK 已能加载真实底图；这只证明本地地图显示，不代表公网部署完成。
- FastAPI 和前端 API 链路已通过合成 Provider 验证；真实步行等时圈尚未执行。
- 服务端 AK 不写入本记录、前端源码、测试产物或提交；只在后端本地配置。
- 部署平台、API 地址、前端域名和 HTTPS 已明确为后续阶段事项；因此 N01 当前仍为部分完成，N09 及后续部署验收暂不启动。
- 本记录采用单人负责制：由当前负责人直接确认结论并记录日期，不要求额外签字流程。

## 完成条件

补齐上述待确认项，记录负责人和日期，并将结论回填任务明细的“实际进展、证据链接、阻塞说明”列。若某项本阶段不做，应明确标记“暂不验收”并写入后续计划。
