# 国定一社区边界比较显式恢复

## 授权与固定条件

用户在账本监控修复后明确要求“继续计划”。本次是一次显式恢复，不是自动重跑。旧 comparison-v1 标签及原始中断产物保持不变，算法、Provider、RateGate 和业务 API 不修改。

父账本：D:/CodexOutputs/guodingyi-boundary-comparison-v2/ledger.json，SHA-256 `8a42614afab51d5631cbe7e8917c92c11bce2e9a5235a5c97374d2091c4d24fc`。其中 82 次预留、81 次确认发送，80 个有效参考和 1 个端点偏移记录。三个算法从未启动。

96 点集合继续使用哈希 `f079d6a312934204a55dfbc6ce74b0968b0971998eaa6f84ffa830351cffb436`。仅补测无发送证据的 15 点：其中一个已有预留占用一次尝试，所以只余一次；另外 14 点各最多两次。已发送的 81 点不重新查询，端点偏移仍保持无效参考。

| 阶段 | 本次新增硬上限 | 已有预留 |
| --- | ---: | ---: |
| 固定点补测 | 29 | 82 |
| 自适应一次 | 400 | 0 |
| 均匀网格一次 | 400 | 0 |
| 扇形一次 | 400 | 0 |
| 合计 | 1229 | 82 |

累计最高 1311 次，不超过原定 1392 次；不为凑预算补调用。新旧账本关联但不覆盖，新增额度不重复计算已有额度。

中心 BD09LL 121.513926,31.313077，900 秒，范围 ±1600 米，关闭扩展；QPS 3、真实并发 1、超时 8 秒、每点累计最多两次尝试、每阶段截止 600 秒、种子 20260911。补测后仍须达到 60 个有效参考且两类各至少 10 个；致命鉴权、限流、配额、参数、记录故障或取消优先停发。

## 工具与离线验证

新增 tools.continue_boundary_comparison，显式校验父账本、原始产物和固定点集哈希；新增 ledger 和 started.marker 禁止第二次恢复。补测用各点剩余额度创建 Scheduler，三算法仍独立缓存，共用同一 Provider 与 RateGate。

监控只消费进程标准输出，运行时不得打开活动 ledger.json。继承参考记录保留采集来源和时间；新参考有独立采集时间，不将跨时段标签伪称同时采集。

```powershell
$env:PYTHONUTF8='1'
$env:PYTHONPYCACHEPREFIX='D:/CodexCaches/baidu-pycache'
$env:PYTHONPATH='backend'
& D:/CodexCaches/baidu-map-algorithm-venv/Scripts/python.exe -m pytest backend/tests life-circle-algorithm/tests -q -o cache_dir=D:/CodexCaches/live-pytest --basetemp=D:/CodexCaches/continuation-v2-tests
& D:/CodexCaches/baidu-map-algorithm-venv/Scripts/python.exe -m tools.continue_boundary_comparison --prepare
# 仅本次恢复首次运行；启动标记存在后不可再次执行。
& D:/CodexCaches/baidu-map-algorithm-venv/Scripts/python.exe -m tools.continue_boundary_comparison --execute-live
```

产物目录：D:/CodexOutputs/guodingyi-boundary-comparison-v2-continuation/。输出新账本、继承与新增参考、逐点评价、三算法几何、指标 JSON/CSV、局部对照图、配置与代码哈希。累计调用与本次调用分开，旧广域结果不混入边界评价。

## 执行记录

执行前恢复点集已固定，完整 Python 回归 259 项通过（2 条既有依赖弃用提示）。实际运行工具提交为 `13802e1378f9487c0ca577c2ff4ed94d84ea33b6`，原始源码快照和配置哈希一致。补测和三算法均完成，运行后未改变算法或追加真实请求。


### 时间与数据来源

本次恢复北京时间 **2026-09-13 18:14:00–18:23:58**，约 597.157 秒。已有 81 个发送记录来自 17:32:21–17:32:55；新增 15 点测时来自 18:14:01–18:14:06，间隔约 42 分钟。报告保留该时间差，不把标签伪称同步采集。所有“参考真值”均为百度步行返回秒数和端点核验结果，不是现场步行核验。

### 调用与限流验收

| 阶段 | 本次预算预留 | 确认发送／完整响应 | 重试 | 耗时秒 | 结果 |
| --- | ---: | ---: | ---: | ---: | --- |
| 15 点补测 | 15 | 15／15 | 0 | 6.093 | 13 个有效，2 个端点偏移 |
| 自适应 | 400 | 400／400 | 0 | 192.125 | partial，budget |
| 均匀网格 | 400 | 400／400 | 0 | 234.891 | partial，budget |
| 扇形 | 247 | 247／247 | 0 | 163.891 | partial，resolution_limit |
| 本次合计 | 1062 | 1062／1062 | 0 | — | 无全局停止 |

加上父账本 **82 次预留、81 次确认发送**，累计为 **1144 次预留、1143 次确认发送**；其中边界参考累计 97 次预留、96 次确认发送。保留旧的 1 次未确认预留，不退还额度。本次 1062≤1229，累计 1144≤1311≤1392。

所有新增请求均 HTTP 200／百度状态 0，最大在途 1，滚动一秒最多 3 次。最短发送间隔 412.546 毫秒，响应完成后最短间隔 352.988 毫秒；限流审计阻止次数 0，无百度限流、鉴权、配额错误或重试。34 个新增端点偏移响应分别为补测 2、自适应 9、均匀 14、扇形 9，均保留为未知证据。

本次未打开活动账本读取进度，写入故障未再发生；记录修复获得本次有限时段的真实验证，不称为长期压力测试。

### 独立参考覆盖

固定 96 点现均有真实发送证据：**93 个有效、3 个端点偏移无效**，有效比例 96.875%。有效点中可达 55、不可达 38，两类覆盖门槛通过。与任一算法采样坐标重合的点数为 **0**，因此三算法使用同一 93 点集合。

有效点四象限 WN/WS/ES/EN 为 26/26/26/15。24 个翻转点对均已测时；第 19 对 1 个无效，第 24 对 2 个无效，其余点对完整有效。各对明细见 audited-result.json 的 pair_coverage。该抽样专门覆盖旧参考标签空间翻转附近，不代表整个社区面积分布；旧广域参考不混入分母。

### 三算法边界集比较

| 方法 | 误纳率（误纳／预测可达） | 漏纳率（含 unknown） | unknown／有效点 | 确认发送 | 耗时秒 |
| --- | --- | --- | --- | ---: | ---: |
| 自适应 | 4/45 = 8.89% | 14/55 = 25.45% | 11/93 = 11.83% | 400 | 192.125 |
| 均匀网格 | 5/48 = 10.42% | 12/55 = 21.82% | 8/93 = 8.60% | 400 | 234.891 |
| 扇形 | 1/38 = 2.63% | 18/55 = 32.73% | 20/93 = 21.51% | 247 | 163.891 |

自适应 TP=41、FP=4，14 个漏纳中 7 个预测为 unknown、7 个预测不可达。均匀 TP=43、FP=5，12 个漏纳中 unknown 5 个、预测不可达 7 个。扇形 TP=37、FP=1，18 个漏纳中 unknown 12 个、预测不可达 6 个。未知可达点始终计入漏纳，没有为了降低误差从评价中删除。

本次均匀网格比自适应少漏纳 2 点、少 3 个 unknown，但多误纳 1 点，耗时多约 42.8 秒。扇形实际调用较少、误纳较少，同时漏纳和未知更多。这是单社区、单次固定顺序、特定边界抽样的结果，不能宣布普遍排名，也不能与旧广域抽样的漏纳率直接比较。

### 几何与质量

| 方法 | 质量／停止原因 | 未知面积占计算范围 | 分量／孔洞 | 提示 |
| --- | --- | --- | --- | --- |
| 自适应 | partial／budget | 13.78% | 4／0 | range_unknown、unfinished_boundary |
| 均匀网格 | partial／budget | 12.96% | 1／0 | range_unknown、unfinished_boundary |
| 扇形 | partial／resolution_limit | 21.83% | 2／0 | star_shape_limitation、range_unknown |

三份输出几何均有效，越出计算范围的面积为 0。自适应、均匀的可达与未知交集为 0；扇形仅有约 4.39e-17 平方度的浮点数值残差。以输出 BD09LL 几何独立重算逐点预测，与运行时局部坐标评价完全一致。

报告不把分量与孔洞数量称为拓扑准确率，也不报告真实面积 IoU 或边界 P95。图中蓝色为算法估计可达面，灰色为未知，绿／红点为参考可达／不可达，灰点为无效参考；没有参考边界线。

### 验收状态

| 项目 | 状态与证据 |
| --- | --- |
| 只补测 15 点、保留 81 个旧发送记录 | 通过；逐点采集来源及父账本哈希 |
| 累计预算和每点剩余尝试限制 | 通过；新旧账本独立统计，未确认预留仍保留 |
| 参考门槛与三算法各一次 | 通过；93 个有效共同参考，三算法完成 |
| QPS、并发、响应间隔 | 通过；1062 次真实请求审计，无超频 |
| Windows 记录修复 | 本次通过；无读句柄监控、无写入故障 |
| 几何和逐点评价独立复算 | 通过；audit-summary.json |
| 完整 Python 回归 | 259 项通过，2 条既有依赖弃用提示 |
| 旧产物、算法与业务 API 不变 | 通过；哈希和 Git 差异复核 |
| 现场核验、更多社区、重复运行、其他预算及消融 | 未执行，属于下一阶段 |

## 重点错误证据与下一步

三算法共同误纳 **点 46**，其参考耗时 **1255 秒**，不是仅差一两秒的阈值问题；三算法共同漏纳 **点 50、88、90、91、92**。这些点优先核对路线和道路入口，当前证据不足以将原因直接归为围墙、桥梁或真实地图偏差。

| 点 ID | BD09LL 经度、纬度 | 参考秒数 | 三算法共同表现 |
| --- | --- | ---: | --- |
| 46 | 121.510956, 31.317749 | 1255 | 误纳 |
| 50 | 121.512388, 31.307453 | 887 | 漏纳（包含 unknown） |
| 88 | 121.519428, 31.315502 | 899 | 漏纳（包含 unknown） |
| 90 | 121.520556, 31.311001 | 895 | 漏纳（包含 unknown） |
| 91 | 121.520485, 31.311215 | 861 | 漏纳（包含 unknown） |
| 92 | 121.520413, 31.31143 | 856 | 漏纳（包含 unknown） |


建议下一步先审查上述 6 点的既有路线和端点证据；需要新请求或现场步行时单列计划与预算。在查明共同错误前，不改评分、不通过调整参考点来改善指标。之后以预先固定的社区、时间顺序和独立参考设计重复实验，再考虑 200/400/800 与消融。

## 交付与复核入口

原始产物：config.json、protocol.json、continuation.json、ledger.json、reference.json、point-evaluation.json、adaptive/uniform/radial.json、metrics.json/csv、result.json 和 comparison.svg/png。新增审计产物：audited-result.json、audited-point-evaluation.json、audited-metrics.json/csv、audit-summary.json、audit_after_run.py、priority-review-points.json、runtime-source 和 security-check.json。图像已用本地浏览器渲染并检查，没有加载真实地图或追加百度请求。

```powershell
# 仅在真实进程退出后运行；纯离线复核
& D:/CodexCaches/baidu-map-algorithm-venv/Scripts/python.exe D:/CodexOutputs/guodingyi-boundary-comparison-v2-continuation/audit_after_run.py
```

本次结果及报告更新至 PR #16，依赖冻结 PR #15；comparison-v1 标签保持 f2a3a76，不移动、不自动合并。旧中断报告保留历史事实，当前完成情况以本报告为准。
